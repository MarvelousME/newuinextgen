# Ecosystem Odoo addons (Community)
# Install ecosystem_* modules per tenant blueprint during provisioning.

Place custom Odoo modules here for tenant-specific business extensions.
Platform API uses JSON-RPC against Odoo — not direct UI exposure.
