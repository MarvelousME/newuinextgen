# AI Integration — File change register

Change entries are appended per file group. Baseline: `00-baseline.md`.

| Change ID | Files | Responsibility | Problem | Planned change | DB impact | Security impact | Privacy impact | Tests | Rollback |
|-----------|-------|----------------|---------|----------------|-----------|-----------------|----------------|-------|----------|
| NGTAI-001 | `includes/class-ngtai-{crypto,signature,nonce-store,idempotency-store,database,migrator,activator,deactivator,config,access}.php`, `config/capabilities.php` | Security + storage core | Legacy canonical string; no encrypted secret storage; no versioned migrations; no durable delivery/approval tables | Directive §5–§9 implementation | 4 tables via dbDelta, versioned `ngtai_db_version` | HMAC §7, replay §8, encrypted secret §6 | none | `tests/test-signature*.php`, `test-nonce-replay.php`, `test-idempotency.php` | Delete package folder; Companion untouched |
| NGTAI-002 | `includes/class-ngtai-{event-envelope,event-mapper,redactor,policy-gate,api-client,outbox-bridge,delivery-repository,result-repository}.php`, `config/{event-schemas,payload-allowlists}.php`, `includes/contracts/*` | Event pipeline | Envelope not §10-shaped; no schema allowlists; no dedicated delivery table processor; no policy gate class | Directive §10–§16 | Reads/writes `ngtai_deliveries`, `ngtai_agent_results` | Fail-closed policy gate; HTTPS + host allowlist client | Recursive redaction, minor minimization, allowlist enforcement | `test-redaction.php`, `test-minor-minimization.php`, `test-event-mapping.php`, `test-outbox-delivery.php`, `test-policy-gate.php` | Delete package folder |
| NGTAI-003 | `nextgentutors-ai-integration.php`, `uninstall.php`, `includes/class-ngtai-{plugin,callback-controller,health,audit,logger,cron,cli}.php`, `includes/admin/*`, `includes/rest/*`, `assets/*`, `readme.txt`, `languages/` | Runtime surface | Callback auth used legacy canonical; no approvals workflow/page; no outbox REST; CLI subset | Directive §4, §17–§24 | Reads all four tables | Machine-callback permission chain (§17); capability + nonce admin | Scrubbed logging only | `test-callback-auth.php` + suite | Delete package folder |
| NGTAI-004 | `tests/*`, `phpunit.xml.dist`, `docs/*`, `composer.json` | Tests + docs | Test matrix incomplete vs §25 | 12 test files + bootstrap + runner + 6 docs | none | Security test evidence | Redaction test evidence | self | Delete package folder |
| NGTAI-001 | Security/storage core (19c843a0) | Complete — canonical v1.1 signature, migrator 1.1.0, crypto, nonce/idempotency stores | Tests reconciled to new canonical string + error codes |
| NGTAI-002 | Event pipeline (a7b5843f) | Complete — 27 event schemas, delivery/result repos, policy gate, outbox bridge | Fixed ranking extraction path in callback controller; removed duplicate `class-ngtai-event-envelope.php` |
| NGTAI-INT | Cross-layer integration | `sanitize_key()` stripped dots from `match.recommendation` | Added `sanitize_action_name()` in callback controller + result repository | none | Prevents silent policy/ranking bypass | none | test-idempotency | revert sanitize_action_name patches |
